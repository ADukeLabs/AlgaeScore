@model List<string>
@{
    ViewBag.Title = "index";
}

<h2>Index</h2>


@foreach (var item in Model)
{
    <img src="@item" alt="Algae Images" />
    //TODO Make an action link to points to a new view of one image

    @*<li>
        @Html.ActionLink("")
    </li>*@
}